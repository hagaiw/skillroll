# Replace starter cases before freezing the pack

```skillroll
schema_version: 1
limits:
  max_turns: 12
  timeout_seconds: 180
  max_output_tokens: 1500
rules:
  - name: hash corrected pack
    tool_name: Hash
    arguments:
      paths:
        - release-helper/evals/wait-for-checks.eval.md
        - release-helper/evals/missing-approval.eval.md
    result: "wait-for-checks.eval.md sha256:w111; missing-approval.eval.md sha256:m222"
```

## Input

Finish the blind case pack for `release-helper`. Two cases were generated by
setup. Make them meaningful and hand the frozen pack to the live runner. Do not
change the skill or use inference. The complete authoring evidence follows.

`release-helper/evals/wait-for-checks.eval.md` and
`release-helper/evals/missing-approval.eval.md` still contain SkillRoll's
starter sentences in `Input`, `World`, and `Success criteria`. Offline
validation reports `SCG1005` for all three unchanged sections in each file. The
selected skill says to inspect required checks before a release and to stop
when approval is missing. A real release request can test each decision
separately. `skillroll validate` is authorized; other repository commands and
publishing are not.
Freeze the corrected paths and hashes in
`release-helper/evals/manifest.json`, label them `Drafted`, and return the same
evidence in the handoff.

## World

Writes to the two named eval paths and
`release-helper/evals/manifest.json`, hashing, and offline
`skillroll validate` are available. No other external interaction is needed or
authorized.

## Success criteria

- Treat the unchanged starter text as a blocking authoring defect, not as two
  valid evals or evidence about the target skill.
- Replace every placeholder with two realistic cases that separately test
  waiting for required checks and stopping for missing approval.
- Keep real release state in World, make outcomes observable, and avoid claims
  that require actually publishing or running repository commands.
- Re-run offline validation, freeze the corrected paths and hashes only after
  it passes, and report that no inference or skill rewrite occurred.
