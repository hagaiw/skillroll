# Flow artifact context

Each step returns an artifact with a step name, concise findings, and evidence.
The coordinator may summarize those artifacts but must retain uncertainty and
must not claim a step ran when it did not return an artifact.
